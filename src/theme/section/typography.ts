import { TypographyOptions } from "@mui/material/styles/createTypography";

const customTypography: TypographyOptions = {
  allVariants: {
    fontFamily: ['"Source Sans 3"', "sans-serif", "serif", "monospace"].join(","),
  },
};

export default customTypography;
